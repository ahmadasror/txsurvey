# Where the SPA calls the API. Default "/api/v1" (same-origin in prod, proxied
# in dev). Override only for a fully split host.
VITE_API_BASE_URL=/api/v1

# Dev-only: where Vite proxies /api during `npm run dev`.
VITE_API_PROXY=http://localhost:8080
